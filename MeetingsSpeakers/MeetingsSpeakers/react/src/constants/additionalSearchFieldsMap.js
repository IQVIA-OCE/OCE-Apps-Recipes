import { NAMESPACE } from './namespacePrefix';

export const ADDITIONAL_SEARCH_FIELDS_MAP = {
  specialty: `${NAMESPACE}Specialty__c`,
  professionalDesignation: `${NAMESPACE}ProfessionalDesignation__c`,
  status: `${NAMESPACE}Status__c`,
};
