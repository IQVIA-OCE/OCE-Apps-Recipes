import { NAMESPACE } from './namespacePrefix';

export const ADDITIONAL_FIELDS = {
  status: `${NAMESPACE}Status__c`,
  meetingRecordTypes: `${NAMESPACE}MeetingRecordTypes__c`,
  meetingType: `${NAMESPACE}MeetingType__c`,
};
